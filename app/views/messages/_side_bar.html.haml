.side-bar
  .side-header
    .side-header__box
      %p.side-header__box--username
        = current_user.name
      %ul.side-header__box__menu
        %li.side-header__box__edit-user
          = link_to edit_user_path(current_user) do
            = icon('fas', 'edit')
        %li.side-header__box__new-groups
          = link_to "#" do
            = icon('fas', 'cog')
  .groups
    .group
      %p.group__name
        techcamp
      %p.group__message
        test
    .group
      %p.group__name
        techexpert
      %p.group__message
        sample
    .group
      %p.group__name
        techmaster
      %p.group__message
        drill
